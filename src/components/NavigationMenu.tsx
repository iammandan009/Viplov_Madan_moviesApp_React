import React from "react";
import { NavLink } from "react-router-dom";

const NavigationMenu = () => {
  <div>
    <NavLink to="/">Movies</NavLink>
    <NavLink to="FavouritesList">Favourites</NavLink>
  </div>;
};

export default NavigationMenu;
